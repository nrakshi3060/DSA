package com.rak.dsa.recursion;

import org.junit.jupiter.api.BeforeEach;
import org.junit.jupiter.api.Test;

import java.util.Arrays;

import static org.assertj.core.api.Assertions.assertThat;


class KeyPadCombinationsTest {
    private KeyPadCombinations keyPadCombinations;

    @BeforeEach
    void setUp() {
        keyPadCombinations = new KeyPadCombinations();
    }

    @Test
    void keyPad() {
//        assertThat(keyPadCombinations.keyPad(23)).isEqualTo(Arrays.asList("ad", "ae", "af", "bd", "be", "bf", "cd", "ce", "cf"));
    }
}